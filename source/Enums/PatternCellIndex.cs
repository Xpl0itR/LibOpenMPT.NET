namespace LibOpenMPT.NET.Enums;

public enum PatternCellIndex : int
{
    Note         = 0,
    Instrument   = 1,
    VolumeEffect = 2,
    Effect       = 3,
    Volume       = 4,
    Parameter    = 5
}